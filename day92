#include <stdio.h>

int main() {
    int n, pos, x;
    int arr[101];

    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    scanf("%d %d", &pos, &x);

    for (int i = n; 
